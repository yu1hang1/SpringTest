<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yh.spirngMVC.dao.UserMapper">
    <resultMap id="UserResult" type="com.yh.spirngMVC.bean.User">
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
    </resultMap>
    <sql id ="User_Column">
    id, username, password
  </sql>
    <select id="getUserByUsername" parameterType="java.lang.String" resultMap="UserResult">
        select
        <include refid="User_Column"/>
        from user1
        where username = #{username}
    </select>


    <insert id="insertUser" parameterType="com.yh.spirngMVC.bean.User" useGeneratedKeys="true" keyProperty="id">
    insert into user1 ( username, password)
    values (#{username}, #{password})
    </insert>


</mapper>